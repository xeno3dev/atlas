[project]
name = "atlas"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "platformdirs>=4.0"
]

[project.scripts]
atlas = "atlas.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"